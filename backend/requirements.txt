# OnePlug Backend Dependencies
#
# 更新依赖版本：
# 1. 更新所有依赖：pip install -r requirements.txt --upgrade
# 2. 生成新的版本锁定：pip freeze > requirements.txt
#
# 注意：生产环境建议使用完全锁定的版本以确保可重复构建

Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.6.0
Flask-Migrate==4.0.5
Flask-CORS==4.0.0
PyMySQL==1.1.0
requests==2.31.0
python-dotenv==1.0.0
